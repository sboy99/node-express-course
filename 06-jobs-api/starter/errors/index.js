const CUSTOM_ERROR = require("./custom-api");
const UNAUTHORIZED = require("./unauthenticated");
const NOT_FOUND = require("./not-found");
const BAD_REQUEST = require("./bad-request");

module.exports = {
  CUSTOM_ERROR,
  UNAUTHORIZED,
  NOT_FOUND,
  BAD_REQUEST,
};
